# Nothing is required. The fixture transport is the default and needs no keys.
#
# The live transport reaches a compile-time allowlist of public sibling
# deployments (see data/upstreams.ts) and needs no credentials either. There is
# no secret in this project to leak.
